# تعليمات إصلاح مشكلة Git وملفات الإعدادات

## المشكلة
تم إضافة ملفات الإعدادات (settings.py) إلى الـ commits بالخطأ، وهذه الملفات تحتوي على معلومات حساسة.

## الحل




cgcjh

kuguku
### الخطوة 1: إزالة ملفات الإعدادات من Git (مع الاحتفاظ بها محلياً)

افتح PowerShell أو Command Prompt في مجلد المشروع وقم بتنفيذ الأوامر التالية:

```bash
# إزالة ملفات الإعدادات من Git tracking (لكن الاحتفاظ بها في جهازك)
git rm --cached backend/settings.py
git rm --cached backend/core/settings.py

# إضافة التغييرات على .gitignore
git add .gitignore
```

### الخطوة 2: تعديل آخر commit (إذا كان ملفات الإعدادات في آخر commit فقط)

```bash
# تعديل آخر commit لإزالة ملفات الإعدادات منه
git commit --amend --no-edit
```

### الخطوة 3: إذا كانت ملفات الإعدادات في commit أقدم (الـ commit الأول)

إذا كانت ملفات الإعدادات في commit أقدم وليس الأخير، يمكنك:

#### الخيار أ: إعادة كتابة التاريخ (Interactive Rebase)

```bash
# عرض آخر 5 commits
git log --oneline -5

# إعادة كتابة آخر commitين (استبدل 2 بعدد الـ commits التي تريد تعديلها)
git rebase -i HEAD~2

# في المحرر الذي سيظهر:
# - ابحث عن السطر الذي يحتوي على commit الذي به ملفات الإعدادات
# - غير "pick" إلى "edit"
# - احفظ وأغلق الملف

# ثم قم بتنفيذ:
git rm --cached backend/settings.py
git rm --cached backend/core/settings.py
git commit --amend --no-edit
git rebase --continue
```

#### الخيار ب: إعادة تعيين الـ commits (Reset) - احذر: سيفقد التغييرات غير المحفوظة

```bash
# عرض الـ commits
git log --oneline

# إعادة تعيين إلى قبل الـ commit الذي يحتوي على ملفات الإعدادات
# استبدل <commit-hash> بـ hash الـ commit الذي تريد العودة إليه
git reset --soft <commit-hash>

# الآن أزل ملفات الإعدادات
git rm --cached backend/settings.py
git rm --cached backend/core/settings.py

# أعد إضافة باقي الملفات (بدون ملفات الإعدادات)
git add .

# أنشئ commit جديد
git commit -m "Update admin interfaces and models with Arabic translations, enhance GPA management features, and improve notification handling. Added new GPA page for students and integrated banner notifications in the header. Adjusted various model fields for better clarity and user experience."
```

### الخطوة 4: Push إلى الريبوزيتوري

بعد إزالة ملفات الإعدادات من الـ commits:

```bash
# إذا لم تكن قد push بعد، فقط قم بـ:
git push

# إذا كنت قد push بالفعل، ستحتاج إلى force push (احذر: استخدم فقط إذا كنت متأكداً)
git push --force
```

## ملاحظات مهمة:

1. ✅ تم إضافة ملفات الإعدادات إلى `.gitignore` بالفعل
2. ⚠️ إذا كنت تعمل مع فريق، استخدم `git push --force-with-lease` بدلاً من `--force`
3. ⚠️ Force push سيغير التاريخ، تأكد من أن لا أحد آخر يعمل على نفس الفرع
4. 🔒 ملفات الإعدادات الآن في `.gitignore` ولن يتم إضافتها في المستقبل

## التحقق من النجاح:

```bash
# تحقق من أن ملفات الإعدادات غير متتبعة
git status

# يجب ألا تظهر ملفات settings.py في القائمة
```

